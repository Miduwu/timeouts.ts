{
  "name": "timeouts.ts-dev",
  "version": "2.0.0",
  "description": "Persistent timeouts that call an event when it expires using quick.db!",
  "main": "dist/index.js",
  "types": "index.d.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "timeout-persistent",
    "settimeout",
    "timeouts",
    "timeout",
    "timeout-anticrash"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "discord.js": "^13.6.0",
    "quick.db": "^7.1.3"
  },
  "devDependencies": {
    "@types/node": "^17.0.23",
    "ts-node": "^10.7.0",
    "typescript": "^4.6.3"
  }
}
